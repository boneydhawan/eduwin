# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page

POST    /register                     com.tekizma.controllers.LoginController.addUser()
GET    /register                     com.tekizma.controllers.LoginController.addUser()
GET   /getAboutUsInfo                com.tekizma.controllers.AboutUsController.getAboutUsInfo()

# Map static resources from the /public folder to the /assets URL path
#GET     /assets/*file               controllers.Assets.at(path="/public", file)
